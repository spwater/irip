# IRIP P2 移交文档 — 专家团执行指南

> **移交日期**: 2026-08-08  
> **移交方**: 工程实践专家（Rex）  
> **执行方**: 专家团  
> **前置完成**: P0 (16 项) + P1 (25 项) 全部完成  
> **项目路径**: /Users/shuipei/Desktop/snowSP/irip  
> **版本**: main @ 874dcbb (v0.8.0)

---

## 已完成项摘要

### P0 (16 项 — 全部完成)

**代码修复 (8 项)**:
- C-1: `stream_ask` 重复调用 `_prepare_ask` — 删除第二次调用
- C-2: `insert_run` 参数不匹配 — 修复参数名+补全参数
- C-3: `approve` 缺部门过滤 — 添加 JOIN 过滤
- C-4: `pickle.loads` RCE — 添加 RestrictedUnpickler
- C-5: 沙箱代码注入 — 添加输入转义
- C-6: Celery 任务无超时 — 全部添加 soft/time_limit
- C-7: 每节点多次 session — 合并 session 上下文
- C-8: 顺序 MinIO 下载 — 并行化 + 批量化

**基础设施 (8 项)**:
- I-1: TLS/HTTPS — nginx 443 ssl + 重定向
- I-2: 安全响应头 — HSTS/X-Content-Type-Options/X-Frame-Options
- I-3: 生产禁用 API 文档 — docs_url=None
- I-4: 依赖漏洞扫描 — pip-audit + trivy + gitleaks CI job
- I-5: Redis 分布式限流 — RedisRateLimiter
- I-6: Docker 日志轮转 — json-file max-size 100m
- I-7: 备份异地存储 — IRIP_BACKUP_REMOTE_TARGET
- I-8: 生产密钥校验 — assert_production_keys()

### P1 (25 项 — 全部完成)

**代码修复 (15 项)**:
- C-9: facts org_id 过滤 — get_fact/search_facts/list_facts 添加 department_id
- C-10: get_next_run_number 竞态 — SELECT ... FOR UPDATE
- C-11: upsert_memory 竞态 — INSERT ON CONFLICT DO UPDATE
- C-12/13: 混合步骤修复 — LLM 接收 Python 输出 + 统一状态管理
- C-14: 审计 department_id — 使用 self._dept_id
- C-15: 引擎泄漏 — _get_superuser_factory 单例
- C-16: 内容哈希 Insight — 添加 content_hash 字段
- C-17: retry_node 下游恢复 — resume(run_id)
- C-18: Docker socket 安全 — 添加安全注释
- C-20: 文件浏览符号链接 — islink 检查
- C-21: list_runs N+1 — get_latest_node_executions 批量
- C-22: identify_candidates N+1 — list_artifacts/steps_by_runs 批量
- C-23: get_fact_data 缓存 — Redis 5 分钟 TTL
- C-43: session factory 单例 — _get_session_factory()

**基础设施 (10 项)**:
- I-9: CD 流水线 — 待实现（见 P2 详述）
- I-10: Sentry — 后端 SDK + 前端 ErrorBoundary
- I-11: 日志聚合 — 待实现（见 P2 详述）
- I-12: 告警 — Grafana 仪表盘 + Alertmanager 规则
- I-13: SAST — bandit CI job
- I-14: Secret 扫描 — gitleaks CI job
- I-15: 容器镜像扫描 — trivy（在 dependency-scan job 中）
- I-16: Redis HA — 待实现（见 P2 详述）
- I-17: DB HA — 待实现（见 P2 详述）
- I-18: 分布式追踪 — 待实现（见 P2 详述）

---

## P2 移交清单（37 项）

### 一、代码改进（25 项）

#### P2-C1: 移除 `print()` 调试语句
- **文件**: `packages/ai/ask_service.py` (多处), converters
- **现状**: 生产代码中残留 `print(f"[TIMING] ...")` 调试输出
- **方案**: 全部替换为 `logging.getLogger(__name__).debug()`
- **验收**: `grep -rn "print(" packages/ apps/` 不返回调试语句（排除 __main__ 块）
- **优先级**: 低风险，纯文本替换

#### P2-C2: `_rls_dept_id` 封装泄漏修复
- **文件**: `apps/api/composition/` 下 20+ 文件
- **现状**: 组合层直接对服务对象的 `_rls_dept_id` 私有属性赋值（30+ 处）
- **方案**: 在 `ScopedSessionMixin` 基类添加 `set_rls_override(dept_id: UUID | None)` 公开方法，组合层调用该方法
- **验收**: `grep -rn "_rls_dept_id" apps/api/composition/` 无结果（应全在基类内部）

#### P2-C3: 清理 shim 文件
- **文件**: 8 个纯 re-export 文件
  - `packages/components/flow_runtime.py` → `flow/flow_runtime.py`
  - `packages/components/flow_validation.py` → `flow/flow_validation.py`
  - `packages/components/flows.py` → `flow/flows.py`
  - `packages/standards/object_graph.py` → `objects/object_graph.py`
  - `packages/standards/object_type_dict.py` → `objects/object_type_dict.py`
  - `packages/research/execution/orchestrator.py` → `orchestrator_core.py`
  - `packages/research/products/products.py` → `product_service.py`
  - `packages/research/publication/publication.py` → 子模块
- **方案**: 更新所有 import 引用到实际位置后删除 shim 文件
- **验收**: `python -c "import packages.components.flow_runtime"` 等旧路径不再可用；ruff + mypy + pytest 通过
- **注意**: 需全量搜索 `from packages.components.flow_runtime import` 等旧路径

#### P2-C4: 重命名 `research/models.py` → `dtos.py`
- **文件**: `packages/research/models.py` (1255 行)
- **现状**: 文件名为 models.py 但实际内容全是 DTO dataclass，易与 ORM models 混淆
- **方案**: 重命名为 `dtos.py` 或 `contracts.py`，更新所有 import
- **验收**: `grep -rn "from packages.research.models import" packages/ apps/` 无结果

#### P2-C5: 拆分胖路由文件
- **文件**: `apps/api/routers/` 下 10 个文件 >500 行
  - `research_products.py` (955 行) / `flows.py` (903 行) / `research_publish.py` (759 行)
  - `research_run.py` (720 行) / `governance.py` (709 行) / `assistant.py` (675 行)
- **方案**: 将 Pydantic 请求/响应模型提取到 `schemas.py`，手动序列化改为 `model_validate`
- **验收**: 单个路由文件 <500 行；功能测试全通过

#### P2-C6: 路由间跨模块导入修复
- **文件**: `apps/api/routers/flows.py:66`
- **现状**: `from apps.api.routers.facts import FactListResponse, FactResponse`
- **方案**: 将共享响应模型提取到 `apps/api/schemas/` 目录
- **验收**: 路由文件间无直接 import

#### P2-C7: 提取重复 not_found 错误处理
- **文件**: `packages/` 多个 service 文件（30+ 处）
- **现状**: 重复的 `if obj is None: raise AppError(code="not_found", ...)` 模式
- **方案**: 创建 `require_found(obj, entity_name, entity_id)` 辅助函数
- **验收**: `grep -rn "code=\"not_found\"" packages/` 返回结果减少 80%+

#### P2-C8: 拆分大文件
- **文件**: 7 个文件 >750 行
  - `research/models.py` (1255) / `ai/numeric/service.py` (999) / `research/entities.py` (980)
  - `research/execution/repository_trusted.py` (925) / `parameters/service.py` (919)
  - `components/registry/registry.py` (853) / `research/service.py` (850)
- **方案**: 按功能域拆分为子模块，保留原文件名做 re-export shim（过渡期）
- **注意**: `research/models.py` 已在 P2-C4 中拆分，可合并

#### P2-C9: `_thinking_enabled` 封装修复
- **文件**: `packages/ai/ask_service.py:247-249`
- **现状**: AskService 直接读写 Provider 的 `_thinking_enabled` 私有属性
- **方案**: 在 AIProvider 基类添加 `thinking_enabled` 公开属性

#### P2-C10: 测试覆盖率 30% → 60%
- **文件**: `pyproject.toml:86`
- **现状**: `fail_under = 30`，实际覆盖率 39%
- **方案**: 逐步提高阈值，优先覆盖权限校验/RLS 隔离/加密签名路径
- **验收**: `--cov-fail-under=60` 通过

#### P2-C11: 收窄裸 `except Exception`
- **文件**: `packages/research/planning/plan_analyzer.py` (10+ 处)
- **现状**: `except Exception` 吞没所有异常
- **方案**: 替换为具体异常类型（SQLAlchemyError, JSONDecodeError 等）

#### P2-C12: `_safe_literal` 改用 `quote_literal`
- **文件**: `packages/common/tenant_guc.py:25-39`
- **现状**: 简单单引号转义，非 PostgreSQL 官方 `quote_literal`
- **方案**: 使用 `sa.select(sa.func.quote_literal(value))` 获取安全引用值

#### P2-C13: CORS 通配符校验
- **文件**: `apps/api/main.py:181-189`
- **现状**: `allow_credentials=True` 但无通配符拒绝
- **方案**: 启动时检查 `IRIP_API_CORS_ORIGINS` 不含 `*`

#### P2-C14: 大文本 JSONB 截断
- **文件**: `packages/research/planning/plan_analyzer.py:194,336`
- **现状**: 完整未截断文本存入 `dag_structure` JSONB
- **方案**: 持久化时截断到 256K，完整数据通过 artifact 存储

#### P2-C15: 消息列表轮询 → SSE
- **文件**: `apps/web/src/features/assistant/assistant-page/hooks/useAssistantQueries.ts:45`
- **现状**: 每 3 秒轮询消息列表
- **方案**: 改用 SSE 或 WebSocket，后端已有 `/api/v1/assistant/stream` SSE 端点

#### P2-C16: 重复组件查询
- **文件**: `apps/web/src/features/components/flow-detail/hooks/useFlowQueries.ts:76-79,124-127`
- **现状**: 相同 API 调用两次（不同 queryKey）
- **方案**: 合并为单次查询或使用相同 queryKey 让 React Query 去重

#### P2-C17: Celery 异常重试分类
- **文件**: `apps/worker/tasks/*.py`
- **现状**: 所有异常被吞没返回 dict，Celery 认为成功
- **方案**: 可重试异常（TimeoutError/ConnectionError）用 `raise self.retry(exc=exc)`

#### P2-C18: `list_plans`/`list_runs` 无 LIMIT
- **文件**: `packages/research/execution/repository_trusted.py:105-280`
- **方案**: 添加 `limit` 参数（默认 50），改用游标分页

#### P2-C19: `node_exec_summaries` O(N²) 改 dict
- **文件**: `packages/components/flow/execution_engine.py:371-388`
- **方案**: 改为 `dict[str, dict]`，用 node_id 做 key

#### P2-C20: 前端 `useMemo` Map 构造
- **文件**: `apps/web/src/features/components/flow-detail/hooks/useFlowQueries.ts`
- **方案**: 用 `useMemo` 包裹 5+ 个 Map 构造

#### P2-C21: Beat 调度频率降低
- **文件**: `apps/worker/celery_app.py:101-105`
- **现状**: `research-promote-queued` 每 5 秒一次
- **方案**: 改为 15-30 秒，或使用 Redis 队列长度检查作为前置条件

#### P2-C22: 前端大组件拆分
- **文件**: 7 个组件 >660 行
  - `DepartmentManagement.tsx` (744) / `EvidencePanel.tsx` (738) / `UsersPage.tsx` (693)
- **方案**: 按功能域提取子组件 + 自定义 hooks

#### P2-C23: `evaluate_expression` 并行解析
- **文件**: `packages/ai/numeric/service.py:119-121`
- **方案**: `asyncio.gather` 并行解析变量

#### P2-C24: 快照数据并行加载
- **文件**: `packages/research/execution/context_builder.py:152-184`
- **方案**: 循环外创建 S3 repo，`asyncio.gather` 并行下载

#### P2-C25: AI 配置 API 掩码修复
- **文件**: `apps/api/routers/ai_config.py:211-223`
- **现状**: 掩码作用于加密值而非明文
- **方案**: 先解密再掩码

---

### 二、基础设施改进（12 项）

#### P2-I1: K8s / IaC 评估
- **现状**: 仅 Docker Compose，无 K8s 清单/Terraform
- **方案**: 评估是否需要 K8s（水平扩展、自动扩缩），如需则编写 Helm Chart 或 Terraform
- **注意**: 需架构师参与决策

#### P2-I2: PgBouncer 连接池代理
- **现状**: 直连 PostgreSQL，每个 Worker 创建独立连接池
- **方案**: 添加 PgBouncer 容器，transaction-level pooling
- **注意**: 与 RLS GUC SET LOCAL 兼容性需验证

#### P2-I3: Redis Sentinel 高可用
- **现状**: 单点 Redis
- **方案**: 配置 Redis Sentinel（1 master + 2 replicas + 3 sentinels）
- **注意**: Outbox 模式已支持 Redis 宕机降级

#### P2-I4: PostgreSQL 高可用
- **现状**: 单实例 PostgreSQL
- **方案**: 评估 Patroni / RDS / Cloud SQL，配置流复制 + 自动故障切换
- **注意**: 需架构师决策

#### P2-I5: MinIO 生命周期策略
- **文件**: compose.yaml MinIO 服务配置
- **方案**: 配置 bucket lifecycle 规则，旧对象自动归档/删除

#### P2-I6: 业务数据保留策略
- **现状**: 审计日志/facts/artifacts 无 TTL
- **方案**: 添加定时任务清理超过 N 天的审计日志，归档旧 artifacts

#### P2-I7: CD 流水线
- **文件**: 新建 `.github/workflows/deploy.yml`
- **方案**: CI 通过后构建镜像 → 推送 registry → 触发 staging 部署 → 手动审批 → 生产部署
- **注意**: 需配置 Docker registry 凭据和部署目标环境

#### P2-I8: 日志聚合系统
- **文件**: compose.yaml + `deployments/monitoring/`
- **方案**: 添加 Loki + Promtail 容器，或 ELK stack
- **注意**: structlog JSON 输出已就绪

#### P2-I9: 分布式追踪
- **方案**: 集成 OpenTelemetry SDK（后端）+ Jaeger UI
- **注意**: 需在 FastAPI + SQLAlchemy + Celery 层注入 trace spans

#### P2-I10: 负载测试入 CI
- **文件**: `tests/performance/k6-smoke.js`
- **现状**: 仅有冒烟测试，未入 CI
- **方案**: 添加渐进负载测试（100/500 并发）+ 1h 浸泡测试

#### P2-I11: 前端性能监控
- **方案**: 集成 Web Vitals (RUM) + Lighthouse CI
- **注意**: ErrorBoundary 已在 P1 创建

#### P2-I12: GDPR 数据导出/删除 API
- **方案**: 添加 `GET /api/v1/account/export`（导出用户数据）和 `DELETE /api/v1/account`（删除用户数据）
- **注意**: 需处理 RLS 关联数据的级联删除/匿名化

---

## 执行建议

### 优先级排序
1. **先做低风险高收益**: P2-C1 (print→log), P2-C13 (CORS校验), P2-C21 (Beat频率), P2-C19 (dict优化), P2-C20 (useMemo)
2. **再做中等风险**: P2-C7 (not_found辅助函数), P2-C11 (收窄except), P2-C3 (清理shim), P2-C18 (添加LIMIT)
3. **最后做大改动**: P2-C5/C8 (拆分文件), P2-C2 (封装修复), P2-C4 (重命名)

### 验证标准
每完成一批 P2 项后运行：
```bash
cd /Users/shuipei/Desktop/snowSP/irip
ruff check apps packages tests                    # 0 errors
ruff format --check apps packages tests             # 0 diffs
mypy packages apps/api apps/worker                  # 0 errors
python -m pytest tests/unit -k "not integration" -q # 0 failed
```

### 注意事项
- 不要修改 P0/P1 已修复的文件除非明确需要
- 保持 `.env.example` 与新增环境变量同步
- 每批改动提交前运行 ruff + pytest 验证
- 新增文件需更新 ruff/mypy 的 include 列表（如有）
- 前端改动需运行 `pnpm tsc --noEmit` + `pnpm test` + `pnpm build`

---

## 关键文件索引

| 文件 | 行数 | 关联 P2 项 |
|------|------|------------|
| `packages/ai/ask_service.py` | 900 | C1, C9, C25 |
| `packages/research/models.py` | 1255 | C4, C8 |
| `packages/research/entities.py` | 980 | C8 |
| `packages/research/execution/repository_trusted.py` | 925 | C8, C18 |
| `packages/research/planning/plan_analyzer.py` | 780 | C11, C14, C24 |
| `packages/research/execution/step_executor.py` | 762 | C12/13 (P1已修) |
| `packages/components/flow/execution_engine.py` | 829 | C17 (P1已修), C19 |
| `packages/components/registry/registry.py` | 853 | C8 |
| `packages/parameters/service.py` | 919 | C8 |
| `packages/research/service.py` | 850 | C8 |
| `packages/common/tenant_guc.py` | 39 | C12 |
| `apps/api/main.py` | 350+ | C13, I7 |
| `apps/api/composition/` (20+ 文件) | — | C2 |
| `apps/api/routers/` (10 文件 >500 行) | — | C5, C6 |
| `apps/worker/celery_app.py` | 577 | C17, C21 |
| `compose.yaml` | 360+ | I1, I3, I5, I8 |
| `.github/workflows/ci.yml` | 640+ | I7, I10 |

---

*本文档由工程实践专家（Rex）于 2026-08-08 编写，移交专家团执行。*
