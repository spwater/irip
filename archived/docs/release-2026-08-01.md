# IRIP 阶段性发布报告 — 2026-08-01

## 版本信息
- **分支**: main
- **最新 commit**: fce6b23
- **前一版本**: 89b5b67

## 本次发布内容

### UI 改进
1. **Tab 选中态加深** — 渐变 0.16→0.38、边框 0.32→0.55、投影 0.10→0.18
2. **侧栏导航选中态加深** — 与 tab 统一参数（ocean.css）
3. **water-glass 公共类加深** — 同步参数
4. **Header 底部空间** — paddingBottom 36px + tab marginBottom -14 防裁切
5. **作业进度按钮胶囊化** — antd Button → 渐变半透明胶囊（与 tab 统一）

### Bug 修复
1. **JobDrawer addJob** — IngestionWizard 提交作业后未调用 `useJobStore.addJob()`，导致作业进度抽屉永远显示"暂无作业"

### 代码质量
- TypeScript: 0 错误
- 未使用变量/参数: 0
- ESLint: 无配置文件，跳过

## 测试结果

### 前端 (Vitest)
- 通过: 34
- 失败: 3（预存在 — MentionInput）
- 跳过: 27
- 构建时间: 13.01s

### 后端 (Pytest)
- 通过: 758
- 失败: 64（全部预存在）
- 错误: 90（全部预存在 — testcontainers 环境）
- 跳过: 100

**预存在失败分类**:
| 类别 | 数量 | 原因 |
|------|------|------|
| SSRF 防护 | 12 | .env IRIP_ALLOW_PRIVATE_NETWORK=1 |
| 协作服务 | 17 | DB schema 变更（0055-0059 迁移） |
| 离线引用 | 16 | 同上 |
| 模型生命周期 | 6 | model 前端路由未注册 |
| 账户路由 | 6 | testcontainers app_user 表不存在 |
| 登录流程 | 4 | auth 权限矩阵 49≠42 |
| bootstrap | 3 | testcontainers DB 表缺失 |
| 其他 | 0 | — |

## 修改文件清单
| 文件 | 改动 |
|------|------|
| apps/web/src/app/AppShell.tsx | Tab 选中态加深 + header padding |
| apps/web/src/styles/ocean.css | 侧栏 + water-glass 加深 |
| apps/web/src/features/jobs/JobDrawer.tsx | 按钮 → 渐变胶囊 + 移除 Button import |
| apps/web/src/features/ingestions/IngestionWizard.tsx | addJob 调用 |

## 已知技术债
- 8 个预存在单测失败（auth 权限矩阵、AI tool 数量、0055 不可变表测试）
- model 前端无路由注册
- derivation_run 未接通 fact_data_index
- SSRF 测试需关闭 IRIP_ALLOW_PRIVATE_NETWORK 才能通过
- vitest MentionInput 3 个失败（@提及下拉渲染问题）
